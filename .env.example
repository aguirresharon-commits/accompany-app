# URL del backend. Si no se define, el front usa http://localhost:4000.
# Importante: tiene que coincidir con el puerto donde corre el server (mirá la terminal al hacer npm run dev).
# Si el server dice "Server running at http://localhost:4002", poné:
# VITE_API_URL=http://localhost:4002
# VITE_API_URL=http://localhost:4000
