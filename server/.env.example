# MongoDB (reemplazar con tu URI real)
MONGODB_URI=mongodb+srv://user:password@cluster0.xxxxx.mongodb.net/control?retryWrites=true&w=majority

# JWT para auth (generar una cadena segura en producción)
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Puerto del servidor
PORT=4000

# CORS: orígenes permitidos (frontend). Por defecto localhost:3000 y 5173 (Vite).
# CORS_ORIGIN=http://localhost:3000,http://localhost:5173

# ========== EMAIL (para "Olvidé mi contraseña") ==========
# Sin estas variables, el token se crea pero NO se envía ningún email.

# URL donde abre la app (el link del email llevará a esta URL + ?token=...)
# En desarrollo: RESET_PASSWORD_LINK_BASE=http://localhost:5173
# En producción: RESET_PASSWORD_LINK_BASE=https://tu-app.com
# RESET_PASSWORD_LINK_BASE=http://localhost:5173

# Gmail:
# 1. Activá "Verificación en 2 pasos" en la cuenta Google.
# 2. Creá una "Contraseña de aplicación": Cuenta Google → Seguridad → Contraseñas de aplicaciones.
# 3. Usá esa contraseña en SMTP_PASS (con o sin espacios, se normalizan).
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=tu-email@gmail.com
# SMTP_PASS=xxxx-xxxx-xxxx-xxxx
# SMTP_FROM=Control <tu-email@gmail.com>

# ========== GOOGLE OAUTH (Login con Google) ==========
# Crear credenciales en: https://console.cloud.google.com/apis/credentials
# Tipo: "Aplicación web". En "URI de redirección autorizados" agregar la misma URL que GOOGLE_REDIRECT_URI.
#
# Obligatorias (sin estas dos, el backend responde "Google OAuth no configurado"):
# GOOGLE_CLIENT_ID=xxx.apps.googleusercontent.com
# GOOGLE_CLIENT_SECRET=xxx
#
# Opcionales: si no se definen, GOOGLE_REDIRECT_URI = http://localhost:PORT/api/auth/google/callback, FRONTEND_URL = http://localhost:3001
# FRONTEND_URL debe ser la URL donde corre el frontend (ej. si Vite usa 3002, poné http://localhost:3002)
# GOOGLE_REDIRECT_URI=http://localhost:4000/api/auth/google/callback
# FRONTEND_URL=http://localhost:3002
