# MongoDB (reemplazar con tu URI real)
MONGODB_URI=mongodb+srv://user:password@cluster0.xxxxx.mongodb.net/control?retryWrites=true&w=majority

# JWT para auth (generar una cadena segura en producción)
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Puerto del servidor
PORT=4000

# CORS: orígenes permitidos (frontend). Por defecto localhost:3000 y 5173 (Vite).
# CORS_ORIGIN=http://localhost:3000,http://localhost:5173

# ========== EMAIL (para que el usuario reciba el correo real de "Olvidé mi contraseña") ==========
# Sin estas variables, el token se crea pero NO se envía ningún email.
# Descomentá y completá con tus datos para que el mensaje llegue al correo del usuario.

# URL donde abre la app (el link del email llevará a esta URL + ?token=...)
# RESET_PASSWORD_LINK_BASE=http://localhost:5173
# En producción: RESET_PASSWORD_LINK_BASE=https://tu-app.com

# Ejemplo con Gmail:
# 1. Activá "Verificación en 2 pasos" en tu cuenta Google.
# 2. Creá una "Contraseña de aplicación": Cuenta Google → Seguridad → Contraseñas de aplicaciones.
# 3. Usá esa contraseña en SMTP_PASS (no tu contraseña normal).
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=tu-email@gmail.com
# SMTP_PASS=contraseña-de-16-caracteres-de-google
# SMTP_FROM=Control <tu-email@gmail.com>
